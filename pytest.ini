[pytest]
asyncio_mode = auto
norecursedirs = archived .git __pycache__ .venv venv node_modules

# Python 3.14 compatibility: disable cache provider to avoid MemoryError on session teardown
# https://github.com/pytest-dev/pytest/issues/13048
addopts = -p no:cacheprovider --tb=short
