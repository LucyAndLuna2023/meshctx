# MeshCtx v3.125.0 实施规格（backlog 定稿）

- 文档: MCTX-PLAN-3.125.0-2026-0906 · 状态: 规格 v0.1（rc1 3.124.2 关闭后开工）
- 依赖: 3.124.2 tag 先行（验证 db9fea8e 后 main 解冻 src）
- 纪律: 加法式提交可回滚; edition 边界/open-core 不变; 每项 tests + 10 语言/docs 联动;
  里程碑合入前按 SOP R0-R7 + 三方送审; 全量基线 3784/59 不回归

## 1. P4 观察项 → 代码闭环（002meshctx f1eef868 P4 + 历轮）

| # | 项 | 落地设计 | 验收 |
|---|---|---|---|
| 3.125-P1 | 审计轨迹防篡改（"审计无签名" P4） | ✅ 已实施 (2026-09-06): `prev_hash` sha256 链 + `audit_seal` 末条封签 (同文件尽力而为) + `audit_chain_ok()` + `/api/org/audit/chain`; 5 处审计均改为先审计后落盘 (修复末次操作审计延迟持久化缺陷); bootstrap 入 org_bootstrap 审计 | ✅ 30 org passed (TestAuditChain×3: 链完整/持久化/中间+末条篡改检出/API eve 403); 全量 3787/59 |
| 3.125-P2 | 记忆 key 前缀碰撞（dept:{id} vs user ns） | ✅ 已实施 (2026-09-06): RESERVED_MEM_OWNERS=dept/system; 公共路由 5 handler (store/search/list/delete-entry/delete-ns) 统一 `_guard_reserved_owner` 400; 内部部门共享记忆走 MemoryService.store('dept:..') 直连不受影响 | ✅ test_memory_api 13 passed (TestReservedOwnerGuard×2: 5 路由全 400 + 正常 owner 200) |
| 3.125-P3 | scope 单一 enforce 层（三资源重复判定） | ✅ 已实施 (2026-09-06): org_governance 新增 `is_org_staff()` + `org_dept_gate_ok()` 单一门控; task_cards_api/routines_api 部门视图门控收敛调用 (行为零变化) | ✅ dept 相关套件 75 passed 原样绿 |
| 3.125-P4 | 读操作写副作用审查 | ✅ 走查通过 (2026-09-06): org_api/routines_api/task_cards_api/memory_api 全路由按段扫描 — 写调用 (save/store/record/delete/purge/audit) 仅存在于 post/patch/delete 路由; GET 段零写副作用 | ✅ 扫描记录 + 现有套件绿 |

## 2. org × plan 联动（功能增量, 进 3.125 候选）

- 组织规模/角色上限按订阅 plan 门控（免费单用户 org 保持现状; team/enterprise 上限配置化）;
  实现放 org_governance `PLAN_LIMITS` + org_api 门控返回 403/提示; 定价页/详情页 Edition Scope 段联动更新
- ⚠️ 边界: 本地免费版不硬断（配额软提示语义, 同 HubQuota 先例）; open-core 源码可见性不变

## 3. 运营项（WP2/WP4, 非代码, 进 release notes 而非代码批次）

- WP2 真实榜提交: swebench run_real 官方提交（003 侧环境/凭据）— ops-owned, 004 提供报告
- WP4 跨机冒烟: swarm 2-worker 跨 002/004 真机 — ops-owned
- 海外发布: 001geo 回执（meshctx_publish_v31241 进行中）

## 4. 版本/发版

- 3.125.0 bump A-J 整批 + CHANGELOG（SOP §4）; 每 P 项独立提交; QA 全绿 → rc1 三方 → tag

— meshctx 004 2026-09-06
