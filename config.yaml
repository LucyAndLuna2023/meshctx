models:
  default: deepseek:v4-pro
  entries:
    deepseek:v4-pro:
      base_url: https://api.deepseek.com
      key: ${DEEPSEEK_API_KEY}
      model: ''
      provider: deepseek
server:
  host: 0.0.0.0
  port: 3002
version: '3.0'
