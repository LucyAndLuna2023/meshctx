"""Platform FS — 开源版 (stub)"""
import os

def windows_to_wsl(path: str) -> str:
    """Windows路径转WSL — 开源版直接返回原路径"""
    return path

def wsl_to_windows(path: str) -> str:
    """WSL路径转Windows — 开源版直接返回原路径"""
    return path
